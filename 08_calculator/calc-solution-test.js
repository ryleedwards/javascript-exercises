const multiply = function (array) {
  return array.length
    ? array.reduce((accumulator, nextItem) => accumulator * nextItem)
    : 0;
};

let array = [1, 3, 4, 5, 6, 7];
let array2;

if (array2) {
  console.log("array has numbers");
} else {
  console.log("array empty");
}
